import SIZES from './sizes';

const direction = 'rtl';

const FONTS = {
  h1: {
    fontFamily: 'OscarFM-Regular',
    fontSize: SIZES.h1,
    lineHeight: SIZES.h1,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: 0,
  },
  h2: {
    fontFamily: 'OscarFM-Regular',
    fontSize: SIZES.h2,
    lineHeight: SIZES.h2,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: 0,
  },
  h3: {
    fontFamily: 'OscarFM-Regular',
    fontSize: SIZES.h3,
    lineHeight: SIZES.h3,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: 0,
  },
  h4: {
    fontFamily: 'OscarFM-Regular',
    fontSize: SIZES.h4,
    lineHeight: SIZES.h4,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: 0,
  },
  h5: {
    fontFamily: 'AlmoniDLAAA',
    fontSize: SIZES.h5,
    lineHeight: SIZES.h5,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: -1.0,
  },
  body1: {
    fontFamily: 'AlmoniDLAAA',
    fontSize: SIZES.body1,
    lineHeight: SIZES.body1,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: -1.0,
  },
  body2: {
    fontFamily: 'AlmoniDLAAA',
    fontSize: SIZES.body2,
    lineHeight: SIZES.body2,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: -1.0,
  },
  body3: {
    fontFamily: 'AlmoniDLAAA',
    fontSize: SIZES.body3,
    lineHeight: SIZES.body3,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: -1.0,
  },
  body4: {
    fontFamily: 'AlmoniDLAAA',
    fontSize: SIZES.body4,
    lineHeight: SIZES.body4,
    writingDirection: direction,
    textAlign: direction === 'rtl' ? 'left' : 'right',
    letterSpacing: -1.0,
  },
  fontBold: {
    fontFamily: 'AlmoniDLAAA-Bold',
  },
};

export default FONTS;
